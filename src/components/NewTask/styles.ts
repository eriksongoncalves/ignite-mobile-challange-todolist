import { StyleSheet } from 'react-native'
// import { theme } from '../../theme'

export const styles = StyleSheet.create({
  container: {
    marginTop: -25,
    flexDirection: 'row',
    alignItems: 'center'
  },
  buttonWrapper: {
    marginLeft: 4
  }
})
